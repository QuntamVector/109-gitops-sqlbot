apiVersion: v1
kind: Service
metadata:
  name: sql-rag-demo
  namespace: sql-rag
  labels:
    app: sql-rag-demo
spec:
  type: LoadBalancer
  selector:
    app: sql-rag-demo
  ports:
    - name: http
      port: 80
      targetPort: 8501