apiVersion: v1
kind: Secret
metadata:
  name: sql-rag-secrets
  namespace: sql-rag
  labels:
    app: sql-rag-demo
type: Opaque
stringData:
  # WARNING: plain Secret — these values are committed to git in cleartext.
  # Demo only. Rotate the RDS password before any real use, and prefer
  # SealedSecrets / External Secrets for anything beyond this demo.
  AUTH_DB_PASSWORD: "quantam1234"
  GROQ_API_KEY: "REPLACE_WITH_YOUR_GROQ_API_KEY"
